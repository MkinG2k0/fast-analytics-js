# База данных
# Параметры пула соединений помогают предотвратить ошибки закрытия соединения
# connection_limit - максимальное количество соединений в пуле
# pool_timeout - время ожидания доступного соединения (в секундах)
DATABASE_URL="postgresql://user:password@localhost:5432/fast_analytics?schema=public&connection_limit=10&pool_timeout=20"

# Для Neon PostgreSQL:
# ⚠️ ВАЖНО: Prisma миграции НЕ работают с pooler URL (содержит "-pooler" в хосте)
# Используйте прямой URL (direct connection) для миграций
# Пример pooler URL (для приложения): postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/db
# Пример прямого URL (для миграций): postgresql://user:pass@ep-xxx.region.aws.neon.tech/db
# Если используете Neon, убедитесь, что в DATABASE_URL указан прямой URL (без "-pooler")
# Или используйте DATABASE_MIGRATE_URL для миграций отдельно (см. ниже)

# Опционально: отдельный URL для миграций Prisma
# Если не указан, используется DATABASE_URL
# DATABASE_MIGRATE_URL="postgresql://user:password@localhost:5432/fast_analytics?schema=public"

# JWT секретный ключ (используйте надежный ключ в продакшене!)
JWT_SECRET="your-secret-key-change-in-production"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret-key-change-in-production"

# Google OAuth
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# Окружение (development/production)
# NODE_ENV="development"

# Upstash Redis (для авторизации и отслеживания онлайн пользователей)
UPSTASH_REDIS_REST_URL="https://your-redis-instance.upstash.io"
UPSTASH_REDIS_REST_TOKEN="your-redis-token"

