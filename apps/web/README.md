# Fast Analytics Web

SaaS сервис для аналитики web-приложений, аналог Sentry / LogRocket / Datadog RUM.

## 📋 О проекте

Fast Analytics — это полнофункциональный веб-интерфейс для мониторинга и аналитики web-приложений. Сервис позволяет отслеживать ошибки, логи, события и посещения страниц в реальном времени.

### Основные возможности

- ✅ **Мониторинг ошибок** — автоматический перехват и отслеживание ошибок браузера через SDK
- ✅ **Аналитика логов** — просмотр всех событий приложения с фильтрацией и поиском
- ✅ **Многопроектность** — управление несколькими проектами аналитики
- ✅ **Командная работа** — приглашение участников в проекты с разными ролями
- ✅ **Детальный просмотр событий** — полная информация о событии с stack trace и контекстом
- ✅ **Аналитика посещений** — статистика посещений страниц с группировкой по URL, дате и времени
- ✅ **API ключи** — безопасная интеграция через API ключи проектов

## 🏗️ Технологический стек

- **Next.js 16** (App Router) — фреймворк для frontend и API
- **React 19** — библиотека для UI
- **TypeScript** — типизация кода
- **Ant Design 5** — UI компоненты
- **Tailwind CSS** — утилиты для стилизации
- **NextAuth.js** — аутентификация
- **Prisma + PostgreSQL** — база данных и ORM
- **TanStack Query** — управление состоянием сервера
- **Zustand** — управление локальным состоянием
- **Feature-Sliced Design** — архитектура фронтенда

## 📁 Структура проекта

Проект следует архитектуре **Feature-Sliced Design (FSD)**:

```text
src/
├── app/                    # Next.js App Router (страницы и API routes)
│   ├── api/               # API endpoints
│   ├── project/           # Страницы проектов
│   ├── projects/          # Страница списка проектов
│   ├── login/             # Страница входа
│   ├── register/          # Страница регистрации
│   ├── event/             # Страница деталей события
│   ├── invite/            # Страница принятия приглашения
│   └── providers/         # Провайдеры (Query, Session, Analytics)
├── page-components/        # Компоненты страниц (Page Layer)
├── widgets/               # Виджеты (Widget Layer)
│   ├── header/            # Шапка приложения
│   ├── sidebar/           # Боковое меню
│   └── logs-table/        # Таблица логов
├── features/              # Фичи (Feature Layer)
│   ├── auth/              # Аутентификация
│   ├── manage-members/    # Управление участниками
│   ├── view-log-details/  # Просмотр деталей события
│   ├── page-visits-analytics/ # Аналитика посещений
│   └── filter-logs/       # Фильтрация логов
├── entities/              # Сущности (Entity Layer)
│   ├── event/             # События
│   ├── project/           # Проекты
│   ├── user/              # Пользователи
│   └── project-member/    # Участники проектов
└── shared/                # Общие модули (Shared Layer)
    ├── api/               # API клиенты
    ├── ui/                # UI компоненты
    └── lib/               # Утилиты
```

## 🚀 Быстрый старт

### Требования

- Node.js >= 18
- PostgreSQL >= 14
- pnpm >= 9.0.0

### Установка

1. Установите зависимости:

```bash
pnpm install
```

2. Настройте переменные окружения:

Скопируйте `example.env` в `.env` и заполните значения:

```bash
cp example.env .env
```

Отредактируйте `.env`:

```env
DATABASE_URL="postgresql://user:password@localhost:5432/fast_analytics?schema=public"
JWT_SECRET="your-secret-key-change-in-production"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret"
```

3. Запустите миграции базы данных:

```bash
pnpm db:push
# или для создания миграции
pnpm db:migrate
```

4. Сгенерируйте Prisma Client:

```bash
pnpm db:generate
```

5. Запустите dev сервер:

```bash
pnpm dev
```

Приложение будет доступно по адресу: <http://localhost:3000>

## 📄 Описание страниц

### 🏠 Главная страница (`/`)

**Публичная страница** — демонстрация возможностей сервиса без авторизации.

**Содержимое:**

- Демонстрационная таблица событий с фильтрацией
- Пример детального просмотра события
- Демонстрация аналитики посещений страниц
- Информация о проекте и SDK

**Компоненты:**

- `DemoHeader` — заголовок демо-страницы
- `DemoEventsTable` — таблица демонстрационных событий
- `EventDetails` — детали события
- `PageVisitsAnalytics` — аналитика посещений

### 🔐 Страница входа (`/login`)

**Публичная страница** для аутентификации пользователей.

**Функциональность:**

- Форма входа с email и паролем
- Валидация полей
- Обработка ошибок аутентификации
- Редирект на страницу проектов после успешного входа

**Компоненты:**

- `LoginForm` — форма входа

### 📝 Страница регистрации (`/register`)

**Публичная страница** для создания нового аккаунта.

**Функциональность:**

- Форма регистрации с email, именем и паролем
- Валидация полей (email, минимальная длина пароля)
- Проверка уникальности email
- Автоматический вход после регистрации

**Компоненты:**

- `RegisterForm` — форма регистрации

### 📊 Страница проектов (`/projects`)

**Защищенная страница** — список всех проектов пользователя.

**Функциональность:**

- Отображение списка проектов в таблице
- Создание нового проекта (модальное окно)
- Переход к логам проекта
- Переход к настройкам проекта
- Пагинация таблицы

**Действия:**

- **Создать проект** — открывает модальное окно с формой
- **Логи** — переход на `/project/{id}/logs`
- **Настройки** — переход на `/project/{id}/settings`

**Компоненты:**

- `ProjectsPage` — основная страница
- Таблица проектов с колонками: название, описание, действия

### 📋 Страница логов проекта (`/project/[id]/logs`)

**Защищенная страница** — просмотр всех событий проекта.

**Функциональность:**

- Таблица событий с фильтрацией:
  - По уровню (error, warning, info, debug)
  - По тексту поиска
  - По URL страницы
  - По ID пользователя
  - По диапазону дат
- Обновление данных
- Сброс фильтров
- Переход к деталям события

**Компоненты:**

- `ProjectLogsPage` — основная страница
- `LogsTable` — таблица логов с фильтрами
- `LogsFilter` — компонент фильтрации

### ⚙️ Страница настроек проекта (`/project/[id]/settings`)

**Защищенная страница** — управление настройками проекта.

**Функциональность:**

1. **Информация о проекте:**
   - Просмотр названия и описания
   - Редактирование (для owner/admin)

2. **Участники проекта:**
   - Приглашение новых участников по email
   - Просмотр активных приглашений
   - Управление участниками (изменение роли, удаление)
   - Роли: owner, admin, member, viewer

3. **API Key:**
   - Просмотр API ключа проекта
   - Копирование ключа в буфер обмена
   - Регенерация ключа (для owner/admin)

4. **Опасная зона:**
   - Удаление проекта (только для owner)
   - Подтверждение удаления

**Права доступа:**

- **Owner** — полный доступ ко всем функциям
- **Admin** — управление участниками и настройками, но не удаление проекта
- **Member** — просмотр настроек
- **Viewer** — только просмотр

**Компоненты:**

- `ProjectSettingsPage` — основная страница
- `InviteMemberForm` — форма приглашения участника
- `InvitationsList` — список приглашений
- `MembersList` — список участников

### 📈 Страница аналитики посещений (`/project/[id]/analytics`)

**Защищенная страница** — аналитика посещений страниц проекта.

**Функциональность:**

- Статистика посещений по URL
- Группировка по дате и времени
- Визуализация данных (графики, таблицы)
- Фильтрация по периодам

**Компоненты:**

- `PageVisitsAnalyticsPage` — основная страница
- `PageVisitsAnalytics` — компонент аналитики

### 🔍 Страница деталей события (`/event/[id]`)

**Защищенная страница** — подробная информация о конкретном событии.

**Функциональность:**

- Полная информация о событии:
  - Уровень (error, warning, info, debug)
  - Сообщение
  - Stack trace ошибки
  - Контекст (URL, user agent, userId)
  - Метаданные
  - Скриншот (если доступен)
- Навигация к другим событиям

**Компоненты:**

- `EventPage` — основная страница
- `EventDetails` — компонент деталей события

### ✉️ Страница принятия приглашения (`/invite/[token]`)

**Публичная страница** — принятие приглашения в проект.

**Функциональность:**

- Просмотр информации о приглашении:
  - Название проекта
  - Роль в проекте
  - Email приглашенного пользователя
  - Информация о пригласившем
- Принятие приглашения
- Проверка статуса приглашения (активно, истекло, принято, отменено)
- Проверка соответствия email

**Логика:**

- Если пользователь не авторизован — редирект на страницу входа
- Если email не совпадает — предупреждение
- Если приглашение истекло/отменено — соответствующее сообщение
- После принятия — редирект на страницу логов проекта

**Компоненты:**

- `AcceptInvitationPage` — основная страница

## 🔌 API Endpoints

### Аутентификация

- `POST /api/auth/register` — регистрация пользователя
- `POST /api/auth/login` — вход пользователя
- `[...nextauth]` — NextAuth endpoints

### Проекты

- `GET /api/projects` — список проектов пользователя
- `POST /api/projects` — создание проекта
- `GET /api/projects/[id]` — получение проекта
- `PATCH /api/projects/[id]` — обновление проекта
- `DELETE /api/projects/[id]` — удаление проекта
- `POST /api/projects/[id]/regenerate-key` — регенерация API ключа

### События

- `GET /api/events` — список событий проекта
- `GET /api/events/[id]` — получение события
- `POST /api/events` — создание события (используется SDK)

### Приглашения

- `GET /api/invitations/user` — список приглашений пользователя
- `GET /api/invitations/[token]` — получение приглашения по токену
- `POST /api/projects/[id]/invitations` — создание приглашения
- `POST /api/invitations/[token]` — принятие приглашения
- `DELETE /api/projects/[id]/invitations/[invitationId]` — отмена приглашения

### Участники

- `GET /api/projects/[id]/members` — список участников проекта
- `PATCH /api/projects/[id]/members/[userId]` — изменение роли участника
- `DELETE /api/projects/[id]/members/[userId]` — удаление участника
- `GET /api/projects/[id]/role` — получение роли текущего пользователя

### Аналитика

- `GET /api/page-visits` — статистика посещений страниц

## 🎨 UI Компоненты

### Общие компоненты (`shared/ui`)

- `Button` — кнопка с вариантами стилей
- `Card` — карточка контента
- Другие переиспользуемые компоненты

### Виджеты

- **Header** — шапка приложения с навигацией и уведомлениями
- **Sidebar** — боковое меню с навигацией по проектам
- **LogsTable** — таблица логов с фильтрацией и пагинацией

## 🔐 Аутентификация и авторизация

### Механизм аутентификации

Используется **NextAuth.js** с JWT стратегией:

- Регистрация через email/password
- Вход через email/password
- Сессии хранятся в JWT токенах

### Система авторизации

Система ролей в проектах:

- **Owner** — владелец проекта, полный доступ
- **Admin** — администратор, управление участниками и настройками
- **Member** — участник, просмотр логов и аналитики
- **Viewer** — наблюдатель, только просмотр

## 📦 Интеграция с SDK

Для отправки событий в Fast Analytics используется SDK `fast-analytics-js`:

```typescript
import { init } from "fast-analytics-js";

init({
  projectKey: "your-project-api-key",
  endpoint: "http://localhost:3000/api/events",
});
```

SDK автоматически:

- Перехватывает ошибки браузера
- Отслеживает посещения страниц
- Управляет сессиями
- Отправляет события батчами

## 🧪 Тестирование

### Unit тесты

```bash
pnpm test
```

### E2E тесты

```bash
pnpm test:e2e
```

### Покрытие кода

```bash
pnpm test:coverage
```

## 🚢 Развертывание

### Production build

```bash
pnpm build
pnpm start
```

### Переменные окружения для production

- `DATABASE_URL` — строка подключения к PostgreSQL
- `JWT_SECRET` — секретный ключ для JWT
- `NEXTAUTH_URL` — URL приложения
- `NEXTAUTH_SECRET` — секретный ключ для NextAuth

## 📚 Дополнительная документация

- [SDK документация](../../packages/sdk-analytics-js/README.md)
- [Главный README](../../README.md)
- [Prisma схема](../../packages/database/prisma/schema.prisma)
