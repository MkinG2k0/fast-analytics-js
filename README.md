# Fast Analytics

SaaS сервис для аналитики web-приложений, аналог Sentry / LogRocket / Datadog RUM.

## 🎯 Описание

Fast Analytics — это полнофункциональный сервис для мониторинга и аналитики web-приложений. Сервис позволяет отслеживать ошибки, логи и события в реальном времени.

### Текущий функционал (MVP)

- ✅ Полная аналитика логов, ошибок и предупреждений
- ✅ Автоматический перехват ошибок браузера через SDK
- ✅ Многопроектность (каждый пользователь может создавать несколько проектов)
- ✅ Совместная работа над проектами — система приглашений и ролей (owner, admin, member, viewer)
- ✅ Фильтрация и поиск по событиям
- ✅ Детальный просмотр событий с stack trace и контекстом
- ✅ Предотвращение дубликатов логов с автоматическим подсчетом повторений (occurrenceCount)
- ✅ Ограничение количества ошибок — автоматическое удаление старых ошибок при превышении лимита (настраивается для каждого проекта через ProjectSettings.maxErrors)
- ✅ Аналитика посещений страниц — отслеживание URL, времени на странице, referrer
- ✅ Настройка периода хранения посещений — автоматическая очистка данных о посещениях старше установленного периода (настраивается через ProjectSettings.visitsRetentionDays, по умолчанию 7 дней)
- ✅ Автоматическая очистка старых посещений через cron job (ежедневно в 2:00 UTC)

### Планируемые функции

- 📸 Скриншот экрана во время ошибки
- 🖱️ Аналитика кликов
- 📊 Аналитика самых посещаемых страниц и времени на них
- ⚡ Скорость загрузки страниц и скорость API-запросов

## 🏗️ Архитектура

Проект построен на основе:

- **Turborepo** — монорепозиторий для управления несколькими пакетами
- **Next.js** (App Router) — основной фреймворк для frontend и API
- **Feature-Sliced Design (FSD)** — архитектура фронтенда
- **Prisma + PostgreSQL** — база данных и ORM (вынесена в отдельный пакет `@repo/database`)
- **Upstash Redis** — для управления сессиями и отслеживания онлайн пользователей
- **Ant Design** — UI библиотека компонентов
- **Tailwind CSS** — утилиты для стилизации

### Структура проекта

```
.
├── apps/
│   └── web/              # Next.js приложение (frontend + API)
│       ├── app/          # Next.js App Router
│       │   ├── api/     # API routes
│       │   └── ...      # Страницы
│       └── src/         # Исходный код по FSD
│           ├── app/     # Инициализация приложения
│           ├── pages/   # Страницы
│           ├── widgets/ # Виджеты (Sidebar, Header, LogsTable)
│           ├── features/ # Фичи (Auth, FilterLogs, ViewLogDetails, PageVisitsAnalytics, ManageMembers)
│           ├── entities/ # Сущности (Project, Event, User)
│           └── shared/  # Общие модули (API, UI, lib)
├── packages/
│   ├── database/       # Пакет базы данных (Prisma schema, migrations, PrismaClient)
│   │   ├── prisma/    # Схема и миграции
│   │   └── src/       # Экспорты пакета
│   ├── eslint-config/  # Конфигурация ESLint
│   ├── typescript-config/ # Конфигурация TypeScript
│   └── sdk-analytics-js/   # SDK для публикации в npm
│       └── src/
│           ├── index.ts     # Главный экспорт
│           ├── transport.ts # Отправка событий
│           ├── interceptors.ts # Перехват ошибок
│           └── session.ts  # Управление сессиями
```

## 🚀 Быстрый старт

### Требования

- Node.js >= 18
- PostgreSQL >= 14
- pnpm >= 9.0.0

### Установка

1. Клонируйте репозиторий:

```bash
git clone <repository-url>
cd fast-analytics
```

2. Установите зависимости:

```bash
pnpm install
```

3. Настройте базу данных:

Скопируйте файл `apps/web/example.env` в `apps/web/.env` и заполните значения:

```bash
cp apps/web/example.env apps/web/.env
```

Затем отредактируйте `apps/web/.env` и укажите свои значения:

```env
DATABASE_URL="postgresql://user:password@localhost:5432/fast_analytics?schema=public&connection_limit=10&pool_timeout=20"
JWT_SECRET="your-secret-key-change-in-production"
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-nextauth-secret"
GOOGLE_CLIENT_ID="your-google-client-id" # Опционально: для Google OAuth
GOOGLE_CLIENT_SECRET="your-google-client-secret" # Опционально: для Google OAuth
UPSTASH_REDIS_REST_URL="https://your-redis-instance.upstash.io" # Для сессий и онлайн пользователей
UPSTASH_REDIS_REST_TOKEN="your-redis-token"
CRON_SECRET="your-cron-secret-key" # Опционально: для защиты cron job очистки посещений
```

**Важно для Neon PostgreSQL:** Prisma миграции не работают с pooler URL. Используйте прямой URL (без `-pooler` в хосте) для `DATABASE_URL` при выполнении миграций.

4. Запустите миграции Prisma:

```bash
cd packages/database
pnpm db:push
# или для создания миграции
pnpm db:migrate
```

5. Prisma Client генерируется автоматически при установке зависимостей (через postinstall hook), но можно запустить вручную:

```bash
cd packages/database
pnpm db:generate
```

6. Запустите dev сервер:

```bash
# Из корня проекта
pnpm dev
```

Приложение будет доступно по адресу: http://localhost:3000

## 📦 SDK

SDK находится в директории `packages/sdk-analytics-js` и может быть опубликован в npm как отдельный пакет.

### Использование SDK

1. Установите SDK:

```bash
npm install fast-analytics-js
```

2. Инициализируйте в вашем приложении:

```typescript
import { init, logError, logWarning } from "fast-analytics-js";

init({
  projectKey: "your-project-api-key",
  endpoint: "http://localhost:3000/api/events",
  // Опционально: включить отслеживание посещений страниц
  trackPageVisits: true,
  // Опционально: включить перехват fetch/XHR запросов
  interceptRequests: true,
});

// Автоматический перехват ошибок уже работает!
// Автоматическое отслеживание посещений страниц (если включено)

// Ручная отправка ошибки
try {
  // ваш код
} catch (error) {
  logError(error, {
    customTags: { section: "checkout" },
  });
}

// Отправка предупреждения
logWarning("Пользователь выполнил необычное действие", {
  userId: "user123",
});
```

**Текущая версия SDK:** 0.6.5

Подробнее см. [README SDK](./packages/sdk-analytics-js/README.md)

### Разработка SDK

```bash
cd packages/sdk-analytics-js
pnpm install
pnpm dev  # watch режим
pnpm build  # сборка
```

### Публикация SDK в npm

```bash
cd packages/sdk-analytics-js
npm version patch|minor|major
npm publish --access public
```

## 🗄️ База данных

### Модели

- **User** — пользователи системы (поддержка OAuth через Google и email/password)
- **Project** — проекты пользователей (каждый проект имеет уникальный API ключ и связь с ProjectSettings)
- **ProjectSettings** — настройки проекта (maxErrors, visitsRetentionDays)
- **ProjectMember** — участники проектов с ролями (owner, admin, member, viewer)
- **ProjectInvitation** — приглашения для совместной работы над проектами
- **Event** — события логирования (ошибки, предупреждения и т.д.) с полем `occurrenceCount` для подсчета повторений
- **PageVisit** — записи о посещениях страниц (URL, pathname, referrer, duration, timestamp)

### Миграции

```bash
cd packages/database
pnpm db:migrate  # Создать новую миграцию
pnpm db:push     # Применить изменения без миграции (для разработки)
pnpm db:studio   # Открыть Prisma Studio
pnpm db:reset    # Сброс БД и применение всех миграций
```

**Использование PrismaClient в коде:**

```typescript
import { PrismaClient } from "@repo/database";

const prisma = new PrismaClient();
```

## 🔐 Аутентификация

Система использует NextAuth.js для аутентификации с поддержкой Google OAuth провайдера и email/password. Данные авторизации сохраняются в базе данных, сессии управляются через Upstash Redis для масштабируемости.

### Провайдеры аутентификации

- **Google OAuth** — вход через Google аккаунт
- **Email/Password** — традиционная регистрация и вход (JWT токены)

### Настройка Google OAuth (опционально)

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект или выберите существующий
3. Включите Google+ API
4. Создайте OAuth 2.0 Client ID:
   - Тип приложения: Web application
   - Authorized redirect URIs: `http://localhost:3000/api/auth/callback/google` (для разработки)
   - Для продакшена добавьте ваш домен
5. Скопируйте Client ID и Client Secret в `.env` файл

### Настройка Upstash Redis

1. Создайте аккаунт на [Upstash](https://upstash.com/)
2. Создайте Redis базу данных
3. Скопируйте REST URL и Token в `.env` файл:
   ```
   UPSTASH_REDIS_REST_URL="https://your-redis-instance.upstash.io"
   UPSTASH_REDIS_REST_TOKEN="your-redis-token"
   ```

### Регистрация и вход

- `/register` — регистрация нового пользователя (email/password)
- `/login` — вход в систему (email/password или Google OAuth)

После успешной аутентификации пользователь перенаправляется на `/projects`.

## 📡 API Endpoints

### Аутентификация

- `POST /api/auth/register` — регистрация
- `POST /api/auth/login` — вход

### Проекты

- `GET /api/projects` — список проектов пользователя
- `POST /api/projects` — создание проекта (автоматически создает ProjectSettings)
- `GET /api/projects/:id` — получение проекта с настройками
- `PATCH /api/projects/:id` — обновление проекта (название, описание, настройки через ProjectSettings)
- `POST /api/projects/:id/regenerate-key` — обновление API ключа

### Участники проектов

- `GET /api/projects/:id/members` — список участников проекта
- `POST /api/projects/:id/invitations` — создание приглашения
- `POST /api/invitations/:token/accept` — принятие приглашения
- `DELETE /api/projects/:id/members/:userId` — удаление участника

### События

- `POST /api/events` — создание события (публичный endpoint для SDK)
  - Требует заголовок `x-api-key` с API ключом проекта
  - Автоматически предотвращает дубликаты по комбинации `url` и `context`
  - При обнаружении дубликата увеличивает счетчик `occurrenceCount` существующего события
  - Автоматически удаляет старые ошибки при превышении лимита `maxErrors` из настроек проекта
- `GET /api/events?projectId=...` — получение событий (требует аутентификации)
- `GET /api/events/:id` — получение конкретного события

### Посещения

- `POST /api/page-visits` — создание записи о посещении (публичный endpoint для SDK)
  - Требует заголовок `x-api-key` с API ключом проекта
  - SDK автоматически группирует посещения и отправляет данные о времени на странице
- `GET /api/page-visits` — получение аналитики посещений (требует авторизации)
  - Параметры: `projectId`, `startDate`, `endDate`, `groupBy` (url/date/hour)
  - Возвращает статистику по посещениям, времени на странице, уникальным пользователям
- `POST /api/page-visits/cleanup` — очистка старых посещений (cron job)
  - Защищен секретным ключом через заголовок `Authorization: Bearer <CRON_SECRET>`
  - Запускается автоматически ежедневно в 2:00 UTC через Vercel Cron
  - Удаляет посещения старше `visitsRetentionDays` из настроек проекта (по умолчанию 7 дней)

## 🎨 Frontend структура (FSD)

Проект следует архитектуре Feature-Sliced Design:

- **app/** — инициализация приложения, провайдеры
- **pages/** — страницы приложения
- **widgets/** — сложные композитные компоненты (Sidebar, Header, LogsTable)
- **features/** — бизнес-фичи (Auth, FilterLogs, ViewLogDetails)
- **entities/** — бизнес-сущности (Project, Event, User)
- **shared/** — переиспользуемый код (API клиенты, UI компоненты, утилиты)

## 🛠️ Разработка

### Запуск в режиме разработки

```bash
pnpm dev
```

### Сборка

```bash
pnpm build
```

### Линтинг

```bash
pnpm lint
```

### Проверка типов

```bash
pnpm check-types
```

## 📝 Скрипты

### Корневой уровень

- `pnpm dev` — запуск всех приложений в dev режиме
- `pnpm build` — сборка всех приложений
- `pnpm lint` — линтинг всех пакетов
- `pnpm check-types` — проверка типов

### apps/web

- `pnpm dev` — запуск Next.js dev сервера
- `pnpm build` — сборка Next.js приложения
- `pnpm test` — запуск unit тестов (Vitest)
- `pnpm test:e2e` — запуск E2E тестов (Playwright)

### packages/database

- `pnpm db:generate` — генерация Prisma Client
- `pnpm db:push` — применение изменений схемы к БД без миграции (для разработки)
- `pnpm db:migrate` — создание и применение миграции
- `pnpm db:studio` — открытие Prisma Studio
- `pnpm db:reset` — сброс БД и применение всех миграций

## 🚢 Деплой

### Требования для продакшена

1. Настройте переменные окружения:
   - `DATABASE_URL` — строка подключения к PostgreSQL (для Neon используйте прямой URL без `-pooler`)
   - `JWT_SECRET` — секретный ключ для JWT (используйте надежный ключ!)
   - `NEXTAUTH_URL` — URL вашего приложения
   - `NEXTAUTH_SECRET` — секретный ключ для NextAuth
   - `UPSTASH_REDIS_REST_URL` — URL вашего Upstash Redis инстанса
   - `UPSTASH_REDIS_REST_TOKEN` — токен для Upstash Redis
   - `CRON_SECRET` — секретный ключ для защиты cron job очистки посещений (опционально)
   - `GOOGLE_CLIENT_ID` и `GOOGLE_CLIENT_SECRET` — для Google OAuth (опционально)

2. Запустите миграции:

```bash
cd packages/database
pnpm db:migrate
```

3. Соберите приложение:

```bash
pnpm build
```

4. Запустите production сервер:

```bash
pnpm start
```

## 📄 Лицензия

MIT

## 🤝 Вклад

Проект находится в активной разработке. Приветствуются любые предложения и pull requests!
