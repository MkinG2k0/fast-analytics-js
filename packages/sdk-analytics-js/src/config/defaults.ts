import {
  DEFAULT_BATCH_SIZE,
  DEFAULT_BATCH_TIMEOUT,
  DEFAULT_ENDPOINT,
} from "../model";

export const getDefaultEndpoint = (): string => DEFAULT_ENDPOINT;

export const getDefaultBatchSize = (): number => DEFAULT_BATCH_SIZE;

export const getDefaultBatchTimeout = (): number => DEFAULT_BATCH_TIMEOUT;

